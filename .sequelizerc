const { resolve } = require('path');

module.exports = {
  config: resolve(__dirname, 'src', 'config', 'config.json'),
  'models-path': resolve(__dirname, 'src', 'app', 'models'),
  'migrations-source-path': resolve(__dirname, 'src', 'database', 'migrations'),
  'migrations-compiled-path': resolve(
    __dirname,
    'dist',
    'database',
    'migrations'
  ),
  'seeders-path': resolve(__dirname, 'src', 'database', 'seeds'),
};
